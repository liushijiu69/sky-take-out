<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">

    <!-- 新增套餐 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal (category_id, name, price, status, description, image,
                             create_time, update_time, create_user, update_user)
        values (#{categoryId}, #{name}, #{price}, #{status}, #{description}, #{image},
                #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <!-- 根据分类id查询套餐的数量 -->
    <select id="countByCategoryId" resultType="java.lang.Integer">
        select count(id) from setmeal where category_id = #{categoryId}
    </select>

    <!-- 根据菜品id集合查询关联的套餐数量 -->
    <select id="countByDishIds" resultType="java.lang.Integer">
        select count(dish_id) from setmeal_dish where dish_id in
        <foreach collection="dishIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 修改套餐 -->
    <update id="update">
        update setmeal
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="price != null">price = #{price},</if>
            <if test="image != null">image = #{image},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
        </set>
        where id = #{id}
    </update>

    <!-- 根据id查询套餐 -->
    <select id="selectById" resultType="com.sky.entity.Setmeal">
        select s.id, s.name, s.category_id, s.price, s.image, s.description, s.status,
               s.create_time, s.update_time, s.create_user, s.update_user,
               c.name as category_name
        from setmeal s
        left join category c on s.category_id = c.id
        where s.id = #{id}
    </select>

    <!-- 根据id集合和状态统计套餐数量 -->
    <select id="countByIdsAndStatus" resultType="java.lang.Integer">
        select count(id) from setmeal where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        and status = #{status}
    </select>

    <!-- 根据id集合批量删除套餐 -->
    <delete id="deleteByIds">
        delete from setmeal where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 套餐分页查询 -->
    <select id="selectByPage" resultType="com.sky.entity.Setmeal">
        select s.id, s.name, s.category_id, s.price, s.image, s.description, s.status,
               s.create_time, s.update_time, s.create_user, s.update_user,
               c.name as category_name
        from setmeal s
        left join category c on s.category_id = c.id
        <where>
            <if test="name != null and name != ''">
                and s.name like concat('%',#{name},'%')
            </if>
            <if test="categoryId != null">
                and s.category_id = #{categoryId}
            </if>
            <if test="status != null">
                and s.status = #{status}
            </if>
        </where>
        order by s.update_time desc
    </select>

</mapper>
